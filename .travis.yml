language: python
python:
  - "2.6"
  - "2.7"
  - "3.4"

install:
  - pip install coveralls
  - python setup.py -q install

# command to run tests
script:
  - coverage run --source=compass setup.py test

after_success:
  coveralls
